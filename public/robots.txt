# Everything is open except the guest list.
#
# This is a request, not a control — a crawler may ignore it and a person can
# type the URL. What actually protects the replies is Row Level Security plus
# the `private.admins` allowlist: the anon key is published in the client bundle
# by design, and an account that is not on the allowlist reads nothing even when
# signed in. See supabase/migrations/0002 and 0003.
#
# /admin is kept out of search three ways, of which this is the weakest:
#   1. `noindex, nofollow` on the page itself
#   2. excluded from sitemap.xml (see astro.config.mjs)
#   3. this line
# The path carries no `/wedding-steeja-arjun` prefix. That was the sub-path of a
# GitHub Pages PROJECT site; this is a USER site (the repository is named
# steejaarjun.github.io), which is served at the root of the account's hostname.
# A stale prefix here would disallow a path that does not exist while leaving
# the real /admin crawlable.
#
# This file is served verbatim from public/ — Astro does not rewrite it, and the
# absolute Sitemap: URL below is not derived from `site` in astro.config.mjs.
# Change the host in one and you must change it in the other by hand.
User-agent: *
Allow: /
Disallow: /admin

Sitemap: https://steejaarjun.github.io/sitemap-index.xml
